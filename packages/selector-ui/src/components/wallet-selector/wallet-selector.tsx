import { Component, h, Prop } from "@stencil/core";
import type { WalletSelectorUIComponent } from "@near-wallet-selector/core";

@Component({
  tag: "wallet-selector",
  styleUrl: "wallet-selector.scss",
  shadow: true,
})
export class WalletSelector implements WalletSelectorUIComponent {

}
