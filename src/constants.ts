import packageJson from "../package.json";

export const LOCAL_STORAGE_SELECTED_WALLET_ID = `${packageJson.name}:selectedWalletId`;
export const LOCAL_STORAGE_LEDGER_WALLET_AUTH_DATA = `${packageJson.name}:ledger-wallet:authData`;

export const MODAL_ELEMENT_ID = "near-wallet-selector-modal";
export const DEFAULT_DERIVATION_PATH = "44'/397'/0'/0'/1'";
